port time
import threading



   # position_thread = threading.Thread(target=position, args=(t1, t2, currentPosition, currentSpeed))
#
 #   import threading
 #   while currentSpeed !=0:
 #   t1 = time.time()

  #  t2 = time.time()
   # threading.Thread(position, args=(t1, t2, currentPosition, currentSpeed))
